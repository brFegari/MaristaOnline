<!doctype html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8" />
    <title>Dashboard de Notas - Marista</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="container">
      <div class="card">
        <h1>Dashboard de Notas</h1>
        <p class="note">Faça login no portal e gere um resumo com piores notas e médias por disciplina. As credenciais não são salvas.</p>
        <% if (error) { %><div class="error"><%= error %></div><% } %>
        <form method="post" action="/scrape">
          <div class="grid">
            <div>
              <label>E-mail ou Matrícula</label>
              <input name="email" type="text" placeholder="email@exemplo.com ou matrícula" required />
            </div>

            <div>
              <label>Senha</label>
              <input name="password" type="password" placeholder="••••••••" required />
            </div>
            <div>
              <label>Média alvo</label>
              <input name="targetAvg" type="number" step="0.01" min="0" max="10" value="<%= defaults.targetAvg %>" />
              <div class="small">Usada para calcular a nota necessária na próxima avaliação (pesos iguais).</div>
            </div>
            <div>
              <label>Pontuação máxima por avaliação</label>
              <input name="maxScore" type="number" step="0.01" min="0" max="10" value="<%= defaults.maxScore %>" />
            </div>
          </div>
          <hr />
          <button type="submit">Entrar e gerar dashboard</button>
        </form>
      </div>
    </div>
  </body>
</html>
